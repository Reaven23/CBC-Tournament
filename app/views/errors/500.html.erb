<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Erreur serveur - TournamentGo</title>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <%= favicon_link_tag "favicon.ico" %>
</head>
<body class="d-flex flex-column min-vh-100">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
        <%= image_tag "logo.png", alt: "TournamentGo", class: "navbar-logo me-2" %>
        <strong>TournamentGo</strong>
      <% end %>
    </div>
  </nav>

  <!-- Error Content -->
  <div class="container-fluid d-flex flex-column justify-content-center align-items-center min-vh-100 bg-light">
    <div class="error-container text-center">
      <!-- Error Icon -->
      <div class="error-icon mb-4">
        <i class="fas fa-exclamation-triangle fa-5x text-warning"></i>
      </div>

      <!-- Error Code -->
      <h1 class="error-code display-1 fw-bold text-danger mb-3">500</h1>

      <!-- Error Message -->
      <h2 class="error-title h3 mb-4">Erreur serveur</h2>
      <p class="error-description lead text-muted mb-5">
        Oups ! Notre équipe technique a commis une faute.<br>
        Nous travaillons déjà sur la correction !
      </p>

      <!-- Action Buttons -->
      <div class="error-actions">
        <%= link_to root_path, class: "btn btn-primary btn-lg me-3 mb-3" do %>
          <i class="fas fa-home me-2"></i>
          Retour à l'accueil
        <% end %>

        <button onclick="window.location.reload()" class="btn btn-outline-primary btn-lg mb-3">
          <i class="fas fa-redo me-2"></i>
          Réessayer
        </button>
      </div>

      <!-- Help Text -->
      <div class="error-help mt-5">
        <p class="text-muted small">
          <i class="fas fa-info-circle me-1"></i>
          Si le problème persiste, contactez-nous
        </p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-light py-4 mt-auto">
    <div class="container text-center">
      <p class="mb-0">&copy; <%= Date.current.year %> TournamentGo. Tous droits réservés.</p>
    </div>
  </footer>
</body>
</html>
