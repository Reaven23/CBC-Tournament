<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Page non trouvée - TournamentGo</title>
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <%= favicon_link_tag "favicon.ico" %>
</head>
<body class="d-flex flex-column min-vh-100">
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
      <%= link_to root_path, class: "navbar-brand d-flex align-items-center" do %>
        <%= image_tag "logo.png", alt: "TournamentGo", class: "navbar-logo me-2" %>
        <strong>TournamentGo</strong>
      <% end %>
    </div>
  </nav>

  <!-- Error Content -->
  <div class="container-fluid d-flex flex-column justify-content-center align-items-center min-vh-100 bg-light">
    <div class="error-container text-center">
      <!-- Error Icon -->
      <div class="error-icon mb-4">
        <i class="fas fa-basketball-ball fa-5x text-primary"></i>
      </div>

      <!-- Error Code -->
      <h1 class="error-code display-1 fw-bold text-primary mb-3">404</h1>

      <!-- Error Message -->
      <h2 class="error-title h3 mb-4">Page non trouvée</h2>
      <p class="error-description lead text-muted mb-5">
        Oops ! Cette page semble avoir disparu du terrain.<br>
        Elle a peut-être été remplacée par un autre match !
      </p>

      <!-- Action Buttons -->
      <div class="error-actions">
        <%= link_to root_path, class: "btn btn-primary btn-lg me-3 mb-3" do %>
          <i class="fas fa-home me-2"></i>
          Retour à l'accueil
        <% end %>

        <%= link_to tournaments_path, class: "btn btn-outline-primary btn-lg mb-3" do %>
          <i class="fas fa-trophy me-2"></i>
          Voir les tournois
        <% end %>
      </div>

      <!-- Help Text -->
      <div class="error-help mt-5">
        <p class="text-muted small">
          <i class="fas fa-info-circle me-1"></i>
          Si vous pensez qu'il s'agit d'une erreur, contactez-nous
        </p>
      </div>
    </div>
  </div>

  <!-- Footer -->
  <footer class="bg-dark text-light py-4 mt-auto">
    <div class="container text-center">
      <p class="mb-0">&copy; <%= Date.current.year %> TournamentGo. Tous droits réservés.</p>
    </div>
  </footer>
</body>
</html>
